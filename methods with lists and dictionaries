#Counting the Items in a List

count = 0
for i in <anylist>:
    count = count + 1
#same as
lengthofthelist = len( <anylist> )


#create a list of specific items in a list

weather_types = ["Rain", "Sunny", "Fog", "Fog-Rain", "Thunderstorm", "Type of Weather"]
weather_type_found = []
for i in  weather_types:
   found = i in new_weather
   weather_type_found.append(found)
print(weather_type_found) 


#Tally up occurances of an item in a list. Use ictipoonary with keys=item and value=count of those items
weather_counts = {}
for item in weather:
    if item in weather_counts:                          # if the item is already in the dic
        weather_counts[item] = weather_counts[item] + 1
    else:
        weather_counts[item] = 1                       # if not make a value and assign the count to 1

