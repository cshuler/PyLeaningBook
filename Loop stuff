# for loops

#syntax
for <i> in <object>:
    <some function>
#performs function on each i, 
- in a list the i represents each item. 
- 



# iterate over a list 
for idx, val in enumerate(data_item_list):
    # idx is the counter starting at 0
    # val is the item in the data_item_list
