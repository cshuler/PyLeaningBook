Regular expressions notes
from (https://regexone.com/lesson/character_ranges)



Special characters


note:
\ in front of any special character turns it back to a normal meaning



.         is wildcard, can represent any singe character

\d        can be used in place of any digit from 0 to 9
  
\D 	  Any Non-digit character

[abc]     Only a, b, or c

[^abc] 	Not a, b, nor c

\w 	Any Alphanumeric character (equivalent to the character range [A-Za-z0-9_] )

{m} 	m Repetitions   (For example, a{3} will match the a character exactly three times) (a{1,3} will match the a character no more than 3 times, but no less than once for example)


* 	Zero or more repetitions

+ 	One or more repetitions

? 	any character is optional to make a match  


space (?), tab (\t), new line (\n),  carriage return (\r)


\s 	Any of the above Whitespaces

\S 	Any Non-whitespace character

^x 	Starts with (whatever x is)

x$ 	Ends with (whatever x is)

(…) 	Capture Group
